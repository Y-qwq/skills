# Live authority snapshot

Observed at `2026-09-01T11:30:00+08:00`:

- The service repository publishes `service-api@20`, not the receipt's `@19`.
- `unreadCount` is now `unreadConversationCount`.
- The service branch is merged into its integration branch.
- The client still consumes `service-api@18` and references `unreadCount`.
