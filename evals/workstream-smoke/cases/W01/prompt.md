# W01 — Start a cross-repository workstream

Use the loaded `workstream` skill to handle this request in an isolated temporary workstreams root:

> 帮我给 Messages 增加 bulk mark read。后端接口和前端都要改，你负责拆解、协调、验证；我只看最终 summary。

The harness supports independent tasks and internal workers. Do not mutate a real repository or remote. Use the fixture as the available workspace inventory, create the workstream context artifacts, and show the task contracts, dependency plan, integration gate, and user-facing summary you would produce.
